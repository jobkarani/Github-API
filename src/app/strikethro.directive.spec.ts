import { StrikethroDirective } from './strikethro.directive';

describe('StrikethroDirective', () => {
  it('should create an instance', () => {
    const directive = new StrikethroDirective();
    expect(directive).toBeTruthy();
  });
});
